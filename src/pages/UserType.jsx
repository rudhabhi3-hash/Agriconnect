import { Link } from 'react-router-dom';
export default function UserType() { const types = [['🌾', 'Farmer', 'Sell smarter with AI-powered recommendations.', '/farmer'], ['🤝', 'FPO', 'Aggregate supply and fulfil larger orders.', '/fpo'], ['🏪', 'Buyer', 'Find reliable aggregated agricultural supply.', '/buyer']]; return <section className="types"><label>CHOOSE YOUR WORKSPACE</label><h1>Built for every link<br />in the agricultural chain.</h1><div className="grid three">{types.map(([icon, title, text, to]) => <Link key={title} to={to}><i>{icon}</i><h2>{title}</h2><p>{text}</p><b>Enter workspace →</b></Link>)}</div></section>; }
