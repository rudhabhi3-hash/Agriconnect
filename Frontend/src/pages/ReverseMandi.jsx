import { Fragment } from 'react'; import { PageHead, FPOList } from '../components/Layout'; import BuyerCard from '../components/BuyerCard'; import AIRecommendation from '../components/AIRecommendation'; import { buyerRequirement } from '../data/buyers';
function Flow() { return <div className="flow">{['🏪 Buyer demand', '✦ AI matching', '🌾 FPO discovery', '⊞ Supply aggregation', '🚚 Logistics', '✓ Fulfilment'].map((item, index) => <Fragment key={item}><b>{item}</b>{index < 5 && <span>→</span>}</Fragment>)}</div>; }
export default function ReverseMandi({ demo }) { return <><PageHead demo={demo} tag="REVERSE MANDI" title="From buyer demand → farmer/FPO supply.">A buyer’s need becomes an intelligent, coordinated fulfilment plan.</PageHead><BuyerCard requirement={buyerRequirement} /><Flow /><FPOList /><AIRecommendation /></>; }
