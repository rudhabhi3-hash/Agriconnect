import { PageHead } from '../components/Layout'; import StatCard from '../components/StatCard'; import LogisticsCard from '../components/LogisticsCard'; import { logisticsSummary } from '../services/logisticsService';
export default function Logistics({ demo }) { const stats = [[logisticsSummary.distance, 'TOTAL DISTANCE'], [logisticsSummary.cost, 'TRANSPORT COST'], [logisticsSummary.delivery, 'EST. DELIVERY'], [logisticsSummary.efficiency, 'ROUTE EFFICIENCY']]; return <><PageHead demo={demo} tag="LOGISTICS COMMAND" title="One coordinated route. Three FPOs.">Shared logistics turns fragmented supply into dependable fulfilment.</PageHead><div className="stats">{stats.map(([a, b]) => <StatCard key={b} a={a} b={b} />)}</div><LogisticsCard /></>; }
