import { useNavigate } from 'react-router-dom';
import { PageHead, FPOList } from '../components/Layout'; import AIRecommendation from '../components/AIRecommendation'; import { buyerRequirement } from '../data/buyers';
export default function BuyerDashboard({ setState, demo }) { const navigate = useNavigate(); const fields = [['Crop', buyerRequirement.crop], ['Quantity', buyerRequirement.quantity], ['Quality', buyerRequirement.quality], ['Destination', buyerRequirement.destination], ['Delivery window', buyerRequirement.deliveryWindow]]; return <><PageHead demo={demo} tag="BUYER WORKSPACE" title="Create demand, secure supply.">Describe your requirement; AgriConnect coordinates the rest.</PageHead><div className="split"><section className="panel"><label>CREATE DEMAND</label><h2>Bulk procurement request.</h2><div className="fields">{fields.map(([label, value]) => <label key={label}>{label}<input value={value} readOnly /></label>)}</div><button className="primary wide" onClick={() => setState(state => ({ ...state, found: true, crop: 'Potato', quantity: '50' }))}>Find supply →</button></section><FPOList /></div><AIRecommendation buyer onAction={() => navigate('/reverse-mandi')} /></>; }
