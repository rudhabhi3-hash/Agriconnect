import { PageHead } from '../components/Layout'; import StatCard from '../components/StatCard'; import ForecastChart from '../components/ForecastChart'; import { forecastStats } from '../data/demoData'; import { cropForecastRows } from '../data/crops';
export default function DemandForecast({ demo }) { return <><PageHead demo={demo} tag="DEMAND INTELLIGENCE" title="Demand Forecast">AI-generated prototype prediction · historical signals + projected demand.</PageHead><div className="filters"><button>Tomato⌄</button><button>Delhi NCR⌄</button><button>Next 7 days⌄</button></div><div className="stats">{forecastStats.map(([a, b, c]) => <StatCard key={b} a={a} b={b} c={c} />)}</div><ForecastChart /><div className="compare">{cropForecastRows.map(([crop, growth, expected]) => <div key={crop}><b>{crop}</b><strong>{growth}</strong><small>{expected} expected</small></div>)}</div></>; }
