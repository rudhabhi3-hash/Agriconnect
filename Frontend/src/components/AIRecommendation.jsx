import { Link } from 'react-router-dom';
export default function AIRecommendation({ buyer = false, onAction, state }) { const crop = state?.crop || 'your crop'; const location = state?.location || 'the selected market'; return <section className="ready"><i>{buyer ? '⊞' : '✦'}</i><div><b>{buyer ? '50 / 50 tonnes can be aggregated' : 'Recommendation ready'}</b><p>{buyer ? 'Grade A potato supply discovered across three FPOs.' : `${location} offers the highest estimated net realization for ${crop} after logistics costs.`}</p></div>{onAction ? <button className="primary" onClick={onAction}>Create fulfilment plan →</button> : <Link className="primary" to="/decision-engine">View AI decision →</Link>}</section>; }
